# Product Card App

Small React + Vite project that renders reusable product cards from an array of objects passed as props.

## Features

- Reusable `Card` component
- Gradient card borders
- Product image, title, description, category, and price
- Data-driven rendering from `data/data.js`

## Run

```bash
npm install
npm run dev
```
